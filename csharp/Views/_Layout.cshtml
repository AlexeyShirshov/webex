@{

}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <meta charset="UTF-8">
    <title>
        WebEx @Html.RenderModule("Pages", view: "title")
    </title>
    @foreach (var str in Html.RenderModulesViewOfType("css"))
    { @str }
</head>
<body>
    @RenderBody()
    
    @foreach (var str in Html.RenderModulesViewOfType("js"))
    { @str }
</body>

</html>