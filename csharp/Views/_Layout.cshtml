@{

}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <meta charset="UTF-8">
    <title>
        WebEx @Html.RenderModule("Pages", view: "title")
    </title>
    @foreach (var str in Html.RenderModules(Contracts.CSSView))
    { @str }
</head>
<body>
    @RenderBody()

    @foreach (var str in Html.RenderModules(Contracts.JavascriptView))
    { @str }
</body>

</html>